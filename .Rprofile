#### -- Packrat Autoloader (version 0.4.8-1) -- ####
source("packrat/init.R")
#### -- End Packrat Autoloader -- ####

# packages used throughout the project
pkgs <- c(
  "data.table",
  "zoo")
lapply(pkgs, library, character.only = TRUE)
print(cat("Attached packages:", paste(pkgs)))
rm(pkgs)

# custom colors
azure <- "#007FFF"
orangebrown <- "#E67300"
lgreen <- "#00eb76"
