# Additional Results: Mutual Fund Scandal

```{r, include = FALSE}
# Setup ------------------------------------------------------------------------

# packages
pkgs <- c(
  "data.table",
  "zoo",
  "knitr",
  "kableExtra"
)
lapply(pkgs, library, character.only = TRUE)
options(knitr.kable.NA = '')

# output format
#out.type <- knitr::opts_knit$get('rmarkdown.pandoc.to')
options(knitr.table.format = "html")

# directories
setwd("..")

# load tables
reg.scandal <- readRDS("tab/reg_scandal.Rds")
tab.scandal <- readRDS("tab/summary_stats_scandal.Rds")

# ----------------------------------------------------------------------
```


```{r snapShot200308, echo = FALSE, message = FALSE, warning = FALSE, digits = 2, format.args = list(big.mark = ","), drop0trailing = TRUE}
# Summary by Scandal Status -----------------------------------------
tab.scandal$by.scandal$results %>%
  kable(type = "html", digits = 2, format.args = list(big.mark = ","), drop0trailing = TRUE,
  caption = tab.scandal$by.scandal$title,
  align = c("l", rep("c", 2)),
  booktabs = TRUE) %>%
  kable_styling(
    position = "center",
    font_size = 11) %>%
  footnote(tab.scandal$by.scandal$caption)
# --------------------------------------------------------------------
```


```{r snapShotHL200308, echo = FALSE, message = FALSE, warning = FALSE, digits = 2, format.args = list(big.mark = ",")}
# Summary by Scandal Status -----------------------------------------

tab.scandal$by.exposure$results %>%
  kable(type = "html",
  caption = tab.scandal$by.exposure$title,
  align = c("l", rep("c", 2)),
  booktabs = TRUE) %>%
  kable_styling(
    position = "center",
    font_size = 11) %>%
  footnote(tab.scandal$by.exposure$caption)
# --------------------------------------------------------------------
```

